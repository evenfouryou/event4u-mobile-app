name: Build SIAE Lettore

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build SiaeBridge
        working-directory: desktop-app/SiaeBridge
        run: |
          echo "=== Compiling SiaeBridge (x86) ==="
          dotnet restore
          dotnet build -c Release
          echo ""
          echo "=== Output files ==="
          dir bin\Release\net472\

      - name: Copy prebuilt DLLs
        shell: pwsh
        run: |
          $outDir = "desktop-app/SiaeBridge/bin/Release/net472"
          
          # Copy libSIAE.dll from prebuilt
          $libSiae = "desktop-app/SiaeBridge/prebuilt/libSIAE.dll"
          if (Test-Path $libSiae) {
            Copy-Item $libSiae "$outDir/libSIAE.dll" -Force
            Write-Host "✓ Copied libSIAE.dll from prebuilt"
          } else {
            # Try attached_assets as fallback
            $libSiae2 = Get-ChildItem "attached_assets" -Filter "libSIAE*.dll" | Select-Object -First 1
            if ($libSiae2) {
              Copy-Item $libSiae2.FullName "$outDir/libSIAE.dll" -Force
              Write-Host "✓ Copied libSIAE.dll from attached_assets"
            } else {
              Write-Host "✗ libSIAE.dll NOT FOUND!"
              exit 1
            }
          }
          
          echo ""
          echo "=== Final bridge files ==="
          Get-ChildItem $outDir | Format-Table Name, Length

      - name: Verify required files
        shell: pwsh
        run: |
          $dir = "desktop-app/SiaeBridge/bin/Release/net472"
          $required = @("SiaeBridge.exe", "libSIAE.dll", "Newtonsoft.Json.dll")
          
          $missing = @()
          foreach ($f in $required) {
            $path = Join-Path $dir $f
            if (Test-Path $path) {
              $size = (Get-Item $path).Length
              Write-Host "✓ $f ($size bytes)"
            } else {
              Write-Host "✗ MISSING: $f"
              $missing += $f
            }
          }
          
          if ($missing.Count -gt 0) {
            Write-Host ""
            Write-Host "ERROR: Missing required files!"
            exit 1
          }

      - name: Install Node dependencies
        working-directory: desktop-app
        run: npm install

      - name: Build Electron app
        working-directory: desktop-app
        run: npm run build

      - name: List output
        shell: pwsh
        run: |
          echo "=== Installer ==="
          Get-ChildItem "desktop-app/dist" -Filter "*.exe" | Format-Table Name, Length

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: SIAE-Lettore-Installer
          path: desktop-app/dist/*.exe

      - name: Upload portable
        uses: actions/upload-artifact@v4
        with:
          name: SIAE-Lettore-Portable
          path: desktop-app/dist/win-unpacked/
